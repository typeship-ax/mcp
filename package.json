{
  "name": "@typeship-ax/mcp",
  "version": "0.22.0",
  "description": "MCP server for typeship.",
  "keywords": [
    "typeship",
    "api",
    "typescript",
    "openapi",
    "mcp",
    "mcp-server",
    "typed"
  ],
  "homepage": "https://typeship.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/typeship-ax/mcp.git"
  },
  "license": "MIT",
  "mcpName": "dev.typeship/typeship",
  "type": "module",
  "exports": {
    "./mcp": {
      "types": "./dist/mcp.d.ts",
      "import": "./dist/mcp.js"
    }
  },
  "bin": {
    "typeship-mcp": "dist/mcp.js"
  },
  "files": [
    "dist",
    "src",
    "api.md",
    "api.json",
    "AGENTS.md",
    "server.json",
    "LICENSE"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "mcp:validate": "mcp-publisher validate server.json",
    "mcp:publish": "mcp-publisher publish server.json",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
