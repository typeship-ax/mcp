# typeship — Cloudflare hosting config for the MCP server.
# Generated by typeship — https://typeship.dev
# Configure an application-owned createMcpHandler entry before deployment.
name = "typeship-mcp"
main = "src/worker.ts"
compatibility_date = "2025-06-01"
compatibility_flags = ["nodejs_compat"]

# Replace main with your application-owned Worker entry. It imports
# createMcpHandler and resolves API credentials for each verified MCP identity.
